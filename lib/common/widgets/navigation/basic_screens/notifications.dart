import 'package:flutter/material.dart';

class BasicNotifications extends StatelessWidget {
  const BasicNotifications({super.key});

  @override
  Widget build(BuildContext context) {
     return const Center(child: Text('Notifications'));
  }
}