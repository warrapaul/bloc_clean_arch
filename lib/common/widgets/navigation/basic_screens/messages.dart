import 'package:flutter/material.dart';

class BasicMessages extends StatelessWidget {
  const BasicMessages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Messags')
    );
  }
}