import 'package:flutter/material.dart';

class CustomBaseFormField extends StatelessWidget{
  const CustomBaseFormField({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}